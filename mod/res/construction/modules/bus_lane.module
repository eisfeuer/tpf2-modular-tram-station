local Matrix = require('modutram_matrix')
local t = require('modutram_types')

function data()
    return {
        cost = {
            price = 20000,
        },
        category = {
            categories = { "tram", },
        },
        description = {
            name = _("module_bus_lane_name"),
            description = _("module_bus_lane_desc"),
            icon = "ui/achievements/ach_first_bus_arrived.tga"
        }, 
        availability = {
            yearFrom = 0,
            yearTo = 0
        },
        type = "eisfeuer_modutram_bus_lane",
        order = {
            value = 7,
        },
        metadata = {
            passenger = true,
        },
        updateFn = function(result, transform, tag, slotId, addModelFn, params)
            if result.station:planning_mode_is_active() then
                result.station.builder:add_model({
                    id = 'icons/modutram_bus.mdl',
                    transf = {
                        2, 0, 0, 0,
                        0, 2, 0, 0,
                        0, 0, 2, 0,
                        transform[13], transform[14], 0.05, 1,
                    }
                })
            end
        end,
        getModelsFn = function()
            return {{
                id = 'icons/modutram_bus.mdl',
                transf = {
                    2, 0, 0, 0,
                    0, 2, 0, 0,
                    0, 0, 2, 0,
                    0, 0, 0, 1,
                }
            }}
        end
    }
end